import React from "react";
import {InputButton, InputText, Search} from "./style";

export const TextField = () => {
  return <InputText/>
};

export const Button = () => {
  return <InputButton>Button</InputButton>
};

export const NavSearch = () => {
  return <Search/>
};
